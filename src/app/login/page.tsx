export default function LoginPage() {
  return (
    <main style={{ maxWidth: 400, margin: "4rem auto", padding: "0 1rem" }}>
      <h1>Iniciar sesion</h1>
      {/* TODO: formulario RHF + Zod que llama a signIn("credentials", ...) */}
    </main>
  );
}
