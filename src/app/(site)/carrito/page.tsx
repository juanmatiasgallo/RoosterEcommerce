export default function CarritoPage() {
  return (
    <main style={{ maxWidth: 600, margin: "0 auto", padding: "2rem 1rem" }}>
      <h1>Tu carrito</h1>
      {/* TODO: listar cartItems del usuario logueado + boton "Ir a pagar"
          que crea la orden y llama a createPreference (Mercado Pago) */}
    </main>
  );
}
